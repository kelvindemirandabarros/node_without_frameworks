import HeroRepository from '../repositories/hero';
import HeroService from '../services/hero';

const generateInstance = ({ filePath }) => {
  // Here goes all db connections.
  const hero_repository = new HeroRepo();
};
